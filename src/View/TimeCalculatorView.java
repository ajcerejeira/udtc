package View;

import Utils.UI;

public class TimeCalculatorView {
    public static void home() {
        UI.title("Time calculator");
    //    TUI.menu(new Option[] {
    //            new Option("S", "Start", () -> start(chronometer)),
    //            new Option("B", "Back", System.out::println),
    //    });
    }
}
